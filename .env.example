# Environment variables for Portfolio Project

# Node.js Backend Configuration
NODE_ENV=production
PORT=4000

# PostgreSQL Database Configuration
# IMPORTANT: For Docker Compose, PGHOST should be the service name of the PostgreSQL container (e.g., 'postgres')
PGUSER=your_db_user
PGPASSWORD=your_db_password
PGDATABASE=your_db_name
PGHOST=postgres
PGPORT=5432

